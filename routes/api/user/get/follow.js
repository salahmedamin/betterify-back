const followRequests = require("./follow/followRequests")
const followers = require("./follow/followers")
const following = require("./follow/following")

module.exports = {
    followRequests,
    followers,
    following
}