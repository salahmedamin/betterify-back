const acceptFollow = require("./follow/acceptFollow")
const rejectFollow = require("./follow/rejectFollow")
const sendFollow = require("./follow/sendFollow")
const unFollow = require("./follow/unFollow")
const deletefollow = require("./follow/deletefollow")

module.exports = {
    acceptFollow,
    rejectFollow,
    unFollow,
    sendFollow,
    deletefollow
}